// all constant api endpoints and other constant things go here
export const domain = 'http://localhost:3001';

export const baseUrl = `${domain}/api/v1/`;

const Title = 'Ticket Booking';
export default Title;
